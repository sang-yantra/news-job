import { BASE_URL } from "../constants/index.js";

const ACTIONS = {
  EVERYTHING: `${BASE_URL}/everything`,
  TOP_HEADLINES: `${BASE_URL}/top-headlines`,
  SOURCES: `${BASE_URL}/top-headlines/sources`,
};
export const COUNTRY = "in";
export default ACTIONS;
